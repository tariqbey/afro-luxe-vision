# Copy to .env and fill in from your Supabase project settings (Project Settings → API).
# Without these the app runs in demo mode: sample videos, device-local Bread wallet.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
